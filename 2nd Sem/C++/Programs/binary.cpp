#include <iostream>
using namespace std;

int binary_to_decimal(int binary, int n)
{
    int i, number;
    for(i = 0; i < n; i++)
    {
        number = i * (2)
    }
};

int decimal_to_binary(int decimal)
{

}

int main()
{
    int binary[20];
    cin >> n;

    for(int i = 0; i < n; i++)
        cin >> binary[i];

    cout << "Binary to Decimal: " << binary_to_decimal(binary, n) << endl;

    int decimal;
    cin >> decimal;
    cout << "Decimal to Binary: " << decimal_to_binary(decimal) << endl;

}