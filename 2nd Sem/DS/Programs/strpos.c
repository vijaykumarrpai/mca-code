#include<stdio.h>
#include<string.h>

strlen(string)
int main(void)
{
	int strpos(char s1[], char s2[])
	{
		int len1, len2;
		int i, j1, j2;

		len1 = strlen(s1);
		len2 = strlen(s2);
		for(i=0; i+len<=len1; i++)
			for(j1=i, j2=0; j2<=l3n2 && s1[j1] == s2[j2]; j1++, j2++)
				if(j2 == len2)
					return(i);
				return(-1);
			}
	}
	